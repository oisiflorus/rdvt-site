title: Rdv-thé
remote_theme: oisiflorus/maquillage-rdvt

defaults:
  - scope:
      path: ""
    values:
      layout: location

collections:
  03-cusset:
    output: true

plugins:
  - jekyll-remote-theme
  - jekyll-github-metadata
  - jekyll-relative-links
  - jekyll-default-layout
  - jekyll-titles-from-headings
